<h1>Editing article</h1>
 
<%= form_tag :theater, url: theater_path(@theater), method: :patch do |f| %>
 
  <% if @theater.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@theater.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @atheater.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
 
   <p>
     <%= label_tag :Nombre %><br>
     <%= text_field :theater, :name %>
   </p>
   <p>
   <%= label_tag :Cantidad %>
  <%= number_field :theater, :quantity%>
  </p>
   
  <p>
    <%= submit_tag "Enviar" %>
  </p>
 
<% end %>
 
<%= link_to 'Back', theater_path %>